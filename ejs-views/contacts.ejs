<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body>
    <ul class="obls">
      <% if (contacts.length) { %> <% contacts.forEach(({ color, img }) => { %>
      <li>
        <<%= color %>/>
        <br />
        <img src="<%= img %>" />
      </li>
      <% }) %> <% } %>
    </ul>
  </body>
</html>
